import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist', 'src-tauri/target']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2022,
      globals: globals.browser,
    },
    rules: {
      // Registries (views, tabs, commands) sit next to their components by design; this is a
      // Tauri app where hot reload falling back to a full reload is fine
      'react-refresh/only-export-components': 'off',
    },
  },
  {
    // Generated by the shadcn CLI from the @coss registry; kept as upstream ships it
    files: ['src/components/ui/**', 'src/hooks/**', 'src/lib/segmented-control.ts'],
    rules: {
      'react-hooks/purity': 'off',
    },
  },
])
